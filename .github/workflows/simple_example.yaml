name: simple workflow example

on:
  push:
    branches:
      - main

jobs:
  Display:
    runs-on: ubuntu-latest
    
    steps:
      - name: Display Message
        run: |
          echo "Hello, World!"

      - name: Display working directory
        run: |
          pwd
          ls -lart
